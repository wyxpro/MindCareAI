# MindCareAI 开发清单 v2
## 第二周期（4.26 - 4.28）

### 一、核心产出
**HTP 综合评估模块重构与“疗愈游戏”空间沉浸式视觉体系交付**
*   **逻辑解耦**：将 HTP（房树人）绘画心理评估从“疗愈”模块剥离，成功迁入“AI 评估”体系，建立了标准化的临床测试入口。
*   **沉浸式 UI**：设计并实现了一套“浅色星空（Light Starry Sky）”风格的疗愈游戏看板，通过 AI 生成原生游戏资产，并完成了从深色沉浸到高屏效紧凑布局的多次设计迭代。

### 二、量化指标
*   **模块重构**：新增 2 个核心页面/模块（`HTPEvaluationPage`, `GameTab`），重构 3 个全局关联组件。
*   **AI 深度参与**：调用 `generate_image` 生产高质量 3D 游戏图标；通过 5+ 次截图视觉反馈实现像素级 UI 调优。
*   **性能/体验**：通过 Layout 优化实现移动端 **100% 屏效**（单屏全显，无感加载），增加交互逻辑 4 处（手势说明、外部游戏跳转等）。
*   **代码提交**：完成约 15 次细粒度功能替换与 Lint 错误全量修复。

### 三、过程复盘与沉淀
#### 1. 环节搞定与工具分工
*   **AI 协作**：AI 负责生成复杂的 CSS 渐变效果、Framer Motion 动效以及 HTP 绘画导出的 Canvas 逻辑；人工负责边界逻辑检查（如 Lint 修复、路径搬运）与整体一致性把控。
*   **视觉产出**：通过 `run_command` 自动化搬运 AI 生成的资源到 `public` 目录，实现了“需求 -> 生成 -> 部署 -> 预览”的闭环。

#### 2. 问题破局
*   **布局冲突**：在尝试实现“全屏背景”时，负边距导致与顶部 Tabs 重叠。
    *   *破局：* 放弃激进的 `mt-neg`，改用 `rounded-t` + 阴影层级（Shadow Layering）建立层级感，既保留了沉浸感又解决了遮挡。
*   **资产引用失效**：初步尝试在代码中直接引用绝对路径导致渲染失败。
    *   *破局：* 建立标准的 `public/assets` 管理规范，通过脚本同步资源。

#### 3. 可复用沉淀
*   **提示词套路**：针对“轻量化/高级感”UI 的 Prompt：`bg-white/60 + backdrop-blur-md + border-white + soft-shadow`。
*   **标准流程**：建立“功能迁移 -> 独立路由封装 -> 入口 UI 适配 -> Lint 自动修复”的标准重构 SOP。

### 四、本周随手记
*   **视觉发现**：在浅色背景下，使用 `#818cf8` 和 `#c084fc` 构成的低透明度径向渐变（Radial Gradient）能极好地还原“繁星点点”的星空感。
*   **AI 心得**：截图分析（Image-to-Code Feedback）在 UI 调优阶段的效率远超文字描述，能够精准捕捉到“图标过大”、“背景过深”等主观视觉瓶颈。
*   **技术点**：Framer Motion 的 `AnimatePresence` 在切换“绘图”与“评估结果”子标签时，配合 `mode="wait"` 可以做出极佳的医疗级平稳过渡效果。
